import { Context, Next } from 'koa';
import Application from '../../../typings/app';
declare const _default: (responses: Application.ConfigResponses) => (ctx: Context, next: Next) => Promise<void>;
export = _default;

import { Context, Next } from 'koa';
import { ConfigResponses } from '../../../typings';
declare const _default: (responses: ConfigResponses) => (ctx: Context, next: Next) => Promise<void>;
export = _default;
